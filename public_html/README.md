This directory is generated by `npm run deploy:prepare`.

- Contents are copied from `build/` after `npm run build`.
- Commit these files and push to GitHub so Cloudways PHP can serve them directly.


